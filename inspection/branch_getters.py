import lakat.storage as lakat_storage

def get_article_id_from_article_name(branch_id: bytes, name: str):
    ## get the branch head


def get_branch_head_id(branch_id: bytes):
    ## get the branch head
    db_get